. option condition problem {autoCollapse: true, markerLocation: true} not show location, row 136

. _autoType find a way without innerHTML that also guarantees correct order (application developer may want images in tooltip), row 405

. removeClass .search-load apparently executed before searchCall!! A BIG MYSTERY!, row 496

. _handleAutoresize Should resize max search box size when map is resized., row 515

