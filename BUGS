. option condition problem {autoCollapse: true, markerLocation: true} not show location

. _autoType find a way without innerHTML that also guarantees correct order (application developer may want images in tooltip)

. removeClass .search-load apparently executed before searchCall!! A BIG MYSTERY!

. _handleAutoresize Should resize max search box size when map is resized.

