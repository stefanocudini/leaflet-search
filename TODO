. start animation after setView or panTo, maybe with map.on('moveend')..., row 16

. add custom icon!		, row 26

. refact animate() more smooth! and use bringToFront(), look at: http://goo.gl/IVLjF, row 88

. use create event 'animateEnd' in SearchMarker , row 110

. add option searchLoc or searchLat,searchLon for remapping json data fields, row 125

. add event callback onFound(latlng), row 126

. add option for persist markerLoc after collapse!, row 138

. add option for case sesitive search, also _showLocation, row 365

. pass key and value to _createTip, when _recordsCache support properties, row 387

. use: throw new Error("my message");on error, row 419

. make new method for ajax requestes using XMLHttpRequest, row 422

. add rnd param or randomize callback name! in recordsFromJsonp, row 434

. implement filter by element type: marker|polyline|circle..., row 444

. caching retRecords while layerSearch not change, controlling on 'load' event, row 445

. return also marker! in _recordsFromLayer, row 446

. implements autype without selection(useful for mobile device), row 458

. important optimization!!! always append data in this._recordsCache, row 545

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 548

. change structure of _recordsCache, row 551

. refact _handleAutoresize now is not accurate, row 587

