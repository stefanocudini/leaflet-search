. important! implements uniq option 'sourceData' that recognizes source type: url,array,callback or layer		, row 34

. implement can do research on multiple sources, row 35

. implement sub property filter for propertyName,propertyLoc like this:  "prop.subprop.title", row 38

. add option for persist markerLoc after collapse!, row 49

. add option collapsed, like control.layers, row 59

. resize _input on map resize, row 88

. add option for case sesitive search, also showLocation, row 306

. use .filter or .map, row 309

. patch! remove on Leaflet stable update include isArray() method, row 365

. verify json[i].hasOwnProperty(propName), row 379

. remove script node after call run, row 385

. add rnd param or randomize callback name! in recordsFromJsonp, row 394

. add rnd param or randomize callback name! in recordsFromAjax			, row 414

. implements autype without selection(useful for mobile device), row 469

. important optimization!!! always append data in this._recordsCache, row 560

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 564

. change structure of _recordsCache, row 567

. refact _handleAutoresize now is not accurate, row 618

. showLocation: start animation after setView or panTo, maybe with map.on('moveend')...	, row 711

. add custom icon!	, row 732

. add inner circle, row 740

. refact animate() more smooth! like this: http://goo.gl/DDlRs, row 796

. use create event 'animateEnd' in SearchMarker , row 818

