. start L.Control.SearchMarker.animation after setView or panTo, maybe with map.on('moveend')..., row 15

. refact L.Control.SearchMarker.animate() more smooth! and use bringToFront(), row 79

. use create event animateEnd in L.Control.SearchMarker , row 101

. add option searchLoc or searchLat,searchLon for remapping fields, row 116

. use: throw new Error("my message");on error, row 362

. add rnd param or randomize callback name! in recordsFromJsonp, row 377

. bind _recordsFromLayer to map events: layeradd layerremove update ecc, row 388

. implement filter by element type: marker|polyline|circle..., row 389

. caching retRecords while layerSearch not change, controlling on 'load' event, row 390

. return also marker! in _recordsFromLayer, row 391

. important optimization!!! always append data in this._recordsCache, row 478

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 482

. refact _handleAutoresize now is not accurate, row 517

. refact _animateCircle more smooth!, row 585

