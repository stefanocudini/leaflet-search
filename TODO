. start animation after setView or panTo, maybe with map.on('moveend')..., row 16

. add custom icon!		, row 26

. refact animate() more smooth! and use bringToFront(), look at: http://goo.gl/IVLjF, row 88

. use create event 'animateEnd' in SearchMarker , row 110

. add option searchLoc or searchLat,searchLon for remapping json data fields, row 125

. add event callback onFound(latlng), row 126

. add option for persist markerLoc after collapse!, row 138

. add option for case sesitive search, also _showLocation, row 365

. pass key and value to _createTip, when _recordsCache support properties, row 387

. make new method for ajax requestes using XMLHttpRequest, row 423

. add rnd param or randomize callback name! in recordsFromJsonp, row 435

. implement filter by element type: marker|polyline|circle..., row 445

. caching retRecords while layerSearch not change, controlling on 'load' event, row 446

. return also marker! in _recordsFromLayer, row 447

. implements autype without selection(useful for mobile device), row 459

. important optimization!!! always append data in this._recordsCache, row 546

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 549

. change structure of _recordsCache, row 552

. refact _handleAutoresize now is not accurate, row 588

