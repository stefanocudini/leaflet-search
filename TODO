. start animation after setView or panTo, maybe with map.on('moveend')..., row 15

. marker: true	//show icon optional, show only circleLoc, row 26

. also this._circleLoc, row 50

. refact L.Control.SearchMarker.animate() more smooth! and use bringToFront(), row 74

. use create event animateEnd in L.Control.SearchMarker , row 96

. add option searchLoc or searchLat,searchLon for remapping fields, row 110

. use: throw new Error("my message");on error, row 356

. add rnd param or randomize callback name! in recordsFromJsonp, row 371

. bind _recordsFromLayer to map events: layeradd layerremove update ecc, row 382

. implement filter by element type: marker|polyline|circle..., row 383

. caching retRecords while layerSearch not change, controlling on 'load' event, row 384

. return also marker! in _recordsFromLayer, row 385

. important optimization!!! always append data in this._recordsCache, row 472

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 476

. refact _handleAutoresize now is not accurate, row 511

. refact _animateCircle more smooth!, row 579

