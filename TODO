Call for good developers:

. implement cache for ajax seaching

. refact _handleAutoresize method

. add options: searchJsonpKey and searchJsonpLoc for remapping fields from jsonp

. add options: searchLimit for limit max results show in tooltip

. bind _recordsFromLayer() to map events layeradd layerd remove update ecc

. use throw new Error("json parse error") on jsonp filtering errors

. filter by element type on searching: marker|polyline|circle...

. caching search results while layerSearch contents not change, controlling on 'load' event

. add option for set delay of Collapse after found results

. start animation after setView panning end, maybe on moveend

. rewrite _animateLocation() more smooth
 
. add options: searchJsonpKey and searchJsonpLoc for remapping fields from jsonp

. searchLimit: 100,	//limit max results show in tooltip

. bind _recordsFromLayer to map events layeradd layerd remove update ecc

. filter map's by element type: marker|polyline|circle...

. caching results while layerSearch not change, controlling on 'load' event

. start animation after setView panning end, maybe on moveend

