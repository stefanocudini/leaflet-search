. start L.Control.SearchMarker.animation after setView or panTo, maybe with map.on('moveend')..., row 15

. add custom icon!		, row 25

. refact L.Control.SearchMarker.animate() more smooth! and use bringToFront(), row 81

. use create event animateEnd in L.Control.SearchMarker , row 103

. add option searchLoc or searchLat,searchLon for remapping json data fields, row 118

. add event callback onFound(latlng), row 119

. add option for persist markerLoc after collapse!, row 130

. add new option: the callback for building the tip content from text argument, row 301

. use: throw new Error("my message");on error, row 374

. add rnd param or randomize callback name! in recordsFromJsonp, row 389

. bind _recordsFromLayer to map events: layeradd layerremove update ecc, row 399

. implement filter by element type: marker|polyline|circle..., row 400

. caching retRecords while layerSearch not change, controlling on 'load' event, row 401

. return also marker! in _recordsFromLayer, row 402

. important optimization!!! always append data in this._recordsCache, row 489

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. , row 493

. change structure of _recordsCache, row 497

. refact _handleAutoresize now is not accurate, row 531

. refact _animateCircle more smooth!, row 601

