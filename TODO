. refact _animateCircle more smooth!

. add option searchLoc or searchLat,searchLon for remapping fields

. extend L.Marker for build new type of marker with circle around

. start animation after setView or panTo, maybe with map.on('moveend')...

. use: throw new Error("my message");on error

. add rnd param or randomize callback name! in recordsFromJsonp

. bind _recordsFromLayer to map events: layeradd layerremove update ecc

. implement filter by element type: marker|polyline|circle...

. caching retRecords while layerSearch not change, controlling on 'load' event

. return also marker! in _recordsFromLayer

. important optimization!!! always append data in this._recordsCache

. here insert function that search inputText FIRST in _recordsCache keys and if not find results.. 

. refact _handleAutoresize now is not accurate

. refact _animateCircle more smooth!

